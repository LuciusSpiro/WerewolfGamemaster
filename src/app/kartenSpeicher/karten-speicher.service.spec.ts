/* import { TestBed } from '@angular/core/testing';

import { KartenSpeicherService } from './karten-speicher.service';

describe('KartenSpeicherService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: KartenSpeicherService = TestBed.get(KartenSpeicherService);
    expect(service).toBeTruthy();
  });
});
 */
